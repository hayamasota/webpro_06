<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>図書管理システム</title>
</head>
<body>
  <h2>蔵書一覧</h2>
  <table border="1">
    <tr><th>ID</th><th>タイトル</th><th>著者</th><th>操作</th></tr>
      <% let number = 0 %>
      <% for( let row of data ) { %>
        <tr>
          <td><%= row.id %></td>
          <td><a href="/books/<%= number %>"><%= row.title %></a></td>
          <td><%= row.author %></td>
          <td>
            <a href="/books/edit/<%= number %>">編集</a> | 
            <a href="/books/confirm_delete/<%= number %>">削除</a>
          </td>
        </tr>
        <% number += 1 %>
      <% } %>
  </table>
  <br>
  <a href="/books/create">新しい本を登録</a>
</body>
</html>